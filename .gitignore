# IDEs
.idea/
.vscode/
.devcontainer/

# Python
venv/
.venv/
__pycache__/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Locally cached ORIS responses. Regenerated automatically; kept out of git so
# a refresh does not show up as a change. In production this lives in the
# mounted data volume and survives restarts.
data/.oris_cache/

# Local secrets (Mapy.com API key)
.env
